--Type your SQL command here.

CREATE TABLE PAISES (
    ID_PAIS    INTEGER NOT NULL,
    NOME_PAIS  VARCHAR(80)
);

ALTER TABLE PAISES ADD CONSTRAINT UNQ1_PAISES UNIQUE (NOME_PAIS);
ALTER TABLE PAISES ADD CONSTRAINT PK_PAISES PRIMARY KEY (ID_PAIS);
